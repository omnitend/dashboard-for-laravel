name: Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
    - uses: actions/checkout@v4

    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Run TypeScript checks
      run: npm run typecheck

    # Build first: the test setup imports the built dist/style.css, so the
    # package must be built before the suite runs.
    - name: Build package
      run: npm run build

    # Tests run in Vitest browser mode (Playwright/Chromium), so the browser
    # binary must be installed in CI before the suite runs.
    - name: Install Playwright browser
      run: npx playwright install --with-deps chromium

    - name: Run tests
      run: npm run test:headless

  php-tests:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        php-version: [8.2, 8.3]
        laravel-version: [11.x, 12.x]

    steps:
    - uses: actions/checkout@v4

    - name: Setup PHP ${{ matrix.php-version }}
      uses: shivammathur/setup-php@v2
      with:
        php-version: ${{ matrix.php-version }}
        extensions: mbstring, pdo, pdo_sqlite
        coverage: none

    - name: Install Composer dependencies
      run: composer install --prefer-dist --no-progress --no-interaction

    - name: Run PHPUnit tests (if available)
      run: |
        if [ -f "phpunit.xml" ]; then
          vendor/bin/phpunit
        else
          echo "No PHPUnit configuration found, skipping PHP tests"
        fi
