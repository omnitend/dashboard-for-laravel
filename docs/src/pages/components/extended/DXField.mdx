---
layout: ../../../layouts/DashboardLayout.astro
title: DXField
---

import ComponentExample from '../../../components/ComponentExample.vue';
import PropsTable from '../../../components/PropsTable.vue';
import SlotsTable from '../../../components/SlotsTable.vue';
import DXFieldExample from '../../../examples/DXFieldExample.vue';
import DXFieldExampleRaw from '../../../examples/DXFieldExample.vue?raw';

# DXField

The single-field renderer that powers `DXForm` and
`DXTable`'s edit modal. It renders one `FieldDefinition` of any type — wired to
a `useForm` instance for value binding, validation state, and error display —
and exposes `#value`, `#span`, `#info`, `#info-popover`, and `#hint` slots for
per-field customisation.

You will usually reach for `DXForm`, but `DXField` is
available directly for one-off fields or custom layouts. It binds via a dot
path (`keyPath`), so it also works for nested values such as repeater rows
(`lines.0.price`).

## Live Examples

<ComponentExample
  client:load
  code={DXFieldExampleRaw}
>
  <DXFieldExample client:load />
</ComponentExample>

## Props

<PropsTable client:load props={[
  { name: 'field', type: 'FieldDefinition', required: 'Yes', default: '-', description: 'The field definition to render (see DXForm for the full schema).' },
  { name: 'form', type: 'UseFormReturn<any>', required: 'Yes', default: '-', description: 'Form instance owning the field\'s data and errors.' },
  { name: 'model', type: 'any', required: 'No', default: 'form.data', description: 'Model passed to predicates (label/hint/when/disabled/readonly).' },
  { name: 'keyPath', type: 'string', required: 'No', default: 'field.key', description: 'Dot path into form.data for the value (e.g. lines.0.price for nested binding).' },
  { name: 'errorKey', type: 'string', required: 'No', default: 'keyPath', description: 'Error key for validation lookups.' },
]} />

## Slots

<SlotsTable client:load slots={[
  { name: 'value', props: 'field, model, value, update', description: 'Replace the built-in input control with custom markup. Call update(v) to write the value.' },
  { name: 'span', props: 'field, model, value, update', description: 'Full-width custom block (used when the field is marked span: true).' },
  { name: 'info', props: 'field, model', description: 'Rich info block rendered always-visible below the field (not the label popover).' },
  { name: 'info-popover', props: '-', description: 'Rich body for the label’s hover/focus info popover (lists, bold, paragraphs). Overrides the plain field.info text.' },
  { name: 'hint', props: 'field, model', description: 'Override the hint text rendered below the field.' },
]} />

## Display-only and password field options

Two `FieldDefinition` options change how a control is rendered rather than what
it holds (the full schema lives on the
[DXForm](/components/extended/DXForm#field-definitions) page):

<PropsTable client:load props={[
  { name: 'plaintext', type: 'boolean | (model) => boolean', required: 'No', default: 'false', description: 'Display the value as static text instead of a control — no border, no input box (Bootstrap’s .form-control-plaintext). Implies read-only. Applies to the text-family inputs (text/email/password/number/url/tel/date/time/datetime) and textarea; other control types ignore it and fall back to their readonly handling.' },
  { name: 'revealable', type: 'boolean', required: 'No', default: 'true', description: 'Only for type: "password". Shows the reveal (eye) toggle that switches the input between masked and plain text. Set false for a bare password input.' },
]} />

`readonly` is unchanged: it still renders a bordered, non-editable control, which
reads as "you could edit this, but can't". Use `plaintext` for values a
profile/settings page shows but never lets you edit.

`revealable` is on by default, so every password field gets the eye toggle
unless you opt out.

```typescript
const fields = [
  // Static text, no input box.
  { key: 'email', label: 'Email', type: 'text', plaintext: true },

  // Editable until the record is verified, then display-only.
  { key: 'vat_number', label: 'VAT number', type: 'text', plaintext: (model) => model.verified },

  // A bare password input, without the reveal toggle.
  { key: 'password', label: 'Password', type: 'password', revealable: false },
];
```

## Extended Component

This is a custom component that extends beyond simple Bootstrap Vue Next wrappers,
providing additional functionality specific to Laravel dashboards.
