---
layout: ../../../layouts/DashboardLayout.astro
title: DXDashboard
---

import ComponentExample from '../../../components/ComponentExample.vue';
import PropsTable from '../../../components/PropsTable.vue';
import SlotsTable from '../../../components/SlotsTable.vue';
import DXDashboardExample from '../../../examples/DXDashboardExample.vue';
import DXDashboardExampleRaw from '../../../examples/DXDashboardExample.vue?raw';

# DXDashboard

Complete dashboard layout component that combines sidebar navigation, top navbar, and content area. Handles all state management and provides extensive customisation through slots.

## Live Examples

<ComponentExample
  client:load
  code={DXDashboardExampleRaw}
  example="DXDashboardExample"
/>

## Props

<PropsTable client:load props={[
  { name: 'navigation', type: 'Navigation', default: 'required', description: 'Navigation structure with groups and items' },
  { name: 'currentUrl', type: 'string', default: 'required', description: 'Current URL path for active state detection' },
  { name: 'title', type: 'string', default: '\'Dashboard\'', description: 'Dashboard title shown in sidebar brand' },
  { name: 'pageTitle', type: 'string', default: '\'\'', description: 'Page title shown in navbar' },
  { name: 'fluid', type: 'boolean', default: 'false', description: 'Render page content full-width and left-aligned instead of the default centred, reading-width column. For wide tables / admin pages' },
  { name: 'contentMaxWidth', type: 'string', default: "'1140px'", description: 'Max width of the centred content column (any CSS length) — a genuine cap so pages don’t stretch to ~2000px on a wide display. Ignored when fluid' },
  { name: 'contentClass', type: 'string', default: '-', description: 'Extra class(es) applied to the content container/column' },
  { name: 'user', type: '{ name: string; email: string } | null', default: 'null', description: 'User object for navbar dropdown' },
  { name: 'searchAlign', type: "'start' | 'center'", default: "'start'", description: 'Horizontal alignment of the navbar search slot content (start = flush left, center = centred), forwarded to DXDashboardNavbar' },
  { name: 'actionsOnMobile', type: "'wrap' | 'hide'", default: "'wrap'", description: 'What the navbar actions slot does below md: wrap moves it to its own full-width row, hide removes it (relocate actions into the page on phones). Forwarded to DXDashboardNavbar' },
  { name: 'storageKey', type: 'string', default: '\'dashboard-sidebar-hidden\'', description: 'LocalStorage key for sidebar state persistence' },
]} />

## Slots

<SlotsTable client:load slots={[
  { name: 'default', props: '-', description: 'Main page content rendered in the content area' },
  { name: 'sidebar-brand', props: '{ collapsed: boolean, title: string, toggleSidebar: () => void, sidebarHidden: boolean }', description: 'Custom brand/logo content for sidebar header' },
  { name: 'sidebar-link', props: '{ item: NavigationItem, isActive: boolean, collapsed: boolean, toggleSidebar: () => void, sidebarHidden: boolean }', description: 'Custom link rendering in sidebar navigation' },
  { name: 'sidebar-footer', props: '{ collapsed: boolean, toggleSidebar: () => void, sidebarHidden: boolean }', description: 'Utility links pinned to the bottom of the sidebar (help, changelog, sign-out, …)' },
  { name: 'navbar-menu-icon', props: '{ toggleSidebar: () => void, sidebarHidden: boolean }', description: 'Custom hamburger menu icon for navbar' },
  { name: 'navbar-search', props: '{ toggleSidebar: () => void, sidebarHidden: boolean }', description: 'Custom search component for navbar' },
  { name: 'navbar-actions', props: '{ pageTitle: string, toggleSidebar: () => void, sidebarHidden: boolean }', description: 'Page-level primary actions (e.g. a Create button). Right-aligned next to the user menu from md up; below, wraps to its own full-width row or is hidden entirely per actionsOnMobile' },
  { name: 'navbar-user-icon', props: '{ initial: string, user: User, toggleSidebar: () => void, sidebarHidden: boolean }', description: 'Custom user icon/avatar in navbar dropdown (defaults to DXUserAvatar)' },
  { name: 'navbar-user-menu', props: '{ user: User, toggleSidebar: () => void, sidebarHidden: boolean }', description: 'Custom user dropdown menu content' },
]} />

Every forwarded `sidebar-*` and `navbar-*` slot also receives `toggleSidebar`
and `sidebarHidden` on top of the underlying slot's own bindings — see
[Controlling the sidebar](#controlling-the-sidebar).

## Features

- **Combined Layout** - Integrates DXDashboardSidebar and DXDashboardNavbar
- **State Management** - Handles sidebar toggle and localStorage persistence
- **Responsive** - Works on mobile and desktop
- **Customisable** - Extensive slot system for customisation
- **Type-Safe** - Full TypeScript support with proper types

## Usage Example

### Basic Usage

```vue
<template>
  <DXDashboard
    :navigation="navigation"
    :current-url="route.path"
    title="My Application"
    :page-title="pageTitle"
    :user="authUser"
  >
    <!-- Your page content here -->
    <h1>Welcome to the Dashboard</h1>
  </DXDashboard>
</template>

<script setup lang="ts">
import { DXDashboard } from '@omnitend/dashboard-for-laravel';

const navigation = [
  {
    label: 'Main',
    items: [
      { label: 'Dashboard', url: '/dashboard' },
      { label: 'Customers', url: '/customers' },
    ],
  },
];

const authUser = { name: 'John Doe', email: 'john@example.com' };
const pageTitle = 'Dashboard';
</script>
```

### With Icons

Navigation items can include icons using unplugin-icons or any Vue component:

```vue
<script setup lang="ts">
import { DXDashboard } from '@omnitend/dashboard-for-laravel';
import IconHome from '~icons/lucide/home';
import IconUsers from '~icons/lucide/users';
import IconSettings from '~icons/lucide/settings';

const navigation = [
  {
    items: [
      { label: 'Dashboard', url: '/dashboard', icon: IconHome },
      { label: 'Customers', url: '/customers', icon: IconUsers },
    ],
  },
  {
    label: 'Settings',
    items: [
      { label: 'Preferences', url: '/settings', icon: IconSettings },
    ],
  },
];
</script>
```

**Icon Setup (unplugin-icons):**

Install packages:
```bash
npm install --save-dev unplugin-icons @iconify-json/lucide
```

Configure in `vite.config.ts`:
```typescript
import Icons from 'unplugin-icons/vite';

export default defineConfig({
  plugins: [
    Icons({
      compiler: 'vue3',
    }),
  ],
});
```

Then import icons using the `~icons/` prefix:
```typescript
import IconName from '~icons/lucide/icon-name';
```

## Custom Branding

```vue
<DXDashboard :navigation="navigation" current-url="/dashboard">
  <template #sidebar-brand="{ collapsed }">
    <div class="d-flex align-items-center">
      <img v-if="!collapsed" src="/logo.svg" alt="Logo" />
      <span v-else>L</span>
    </div>
  </template>

  <!-- content -->
</DXDashboard>
```

## Controlling the sidebar

DXDashboard owns the sidebar's visibility state (and persists it to
localStorage), so it exposes that state rather than leaving you to click the
navbar's hamburger from script.

Every forwarded `sidebar-*` and `navbar-*` slot receives two extra bindings:

- **`toggleSidebar`** — shows/hides the sidebar (the same action as the navbar's
  hamburger).
- **`sidebarHidden`** — whether the sidebar is currently hidden.

Use them to put a close affordance in the sidebar itself, or a toggle among the
navbar actions:

```vue
<DXDashboard :navigation="navigation" current-url="/dashboard">
  <template #sidebar-brand="{ collapsed, toggleSidebar }">
    <div class="d-flex align-items-center justify-content-between w-100">
      <img v-if="!collapsed" src="/logo.svg" alt="Logo" />
      <DButton
        variant="link"
        aria-label="Close sidebar"
        @click="toggleSidebar"
      >
        &times;
      </DButton>
    </div>
  </template>

  <template #navbar-actions="{ toggleSidebar, sidebarHidden }">
    <DButton size="sm" variant="outline-secondary" @click="toggleSidebar">
      {{ sidebarHidden ? 'Show menu' : 'Hide menu' }}
    </DButton>
  </template>
</DXDashboard>
```

The same API is available on a template `ref`, for page content or a keyboard
shortcut that sits outside those slots:

```vue
<script setup lang="ts">
import { ref } from 'vue';
import { DButton, DXDashboard } from '@omnitend/dashboard-for-laravel';

const dashboard = ref(null);

const toggle = () => dashboard.value?.toggleSidebar();
</script>

<template>
  <DXDashboard ref="dashboard" :navigation="navigation" current-url="/dashboard">
    <DButton @click="toggle">
      {{ dashboard?.sidebarHidden ? 'Show menu' : 'Hide menu' }}
    </DButton>
  </DXDashboard>
</template>
```

## Notes

This component requires CSS for sidebar visibility control. Add this to your global styles or layout:

```css
.dashboard-sidebar {
  display: none !important;
}

html.sidebar-visible .dashboard-sidebar {
  display: block !important;
}
```

For Astro projects with SSR, add an inline script to prevent flicker:

```html
<script is:inline>
  (function() {
    try {
      const hidden = localStorage.getItem('dashboard-sidebar-hidden');
      if (hidden === 'false') {
        document.documentElement.classList.add('sidebar-visible');
      }
    } catch (e) {}
  })();
</script>
```
