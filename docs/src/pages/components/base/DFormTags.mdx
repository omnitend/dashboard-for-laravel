---
layout: ../../../layouts/DashboardLayout.astro
title: DFormTags
description: Type-safe wrapper around Bootstrap Vue Next's BFormTags component
category: Base Component
tags: wrapper, forms
---

import ComponentExample from '../../../components/ComponentExample.vue';
import BootstrapVueNextReference from '../../../components/BootstrapVueNextReference.vue';
import DFormTagsExample from '../../../examples/DFormTagsExample.vue';

# DFormTags

Type-safe wrapper around Bootstrap Vue Next BFormTags component. Lightweight custom tagged input with duplicate detection and validation.

## Live Examples

<ComponentExample
  client:only="vue"
  code={`<DFormTags
  v-model="tags"
  placeholder="Add tags..."
  duplicate-tag-text="Duplicate tag!"
/>
<p class="mt-2">Tags: <strong>{{ tags.join(", ") }}</strong></p>`}
>
  <DFormTagsExample client:load />
</ComponentExample>


<BootstrapVueNextReference client:load componentName="DFormTags" />
