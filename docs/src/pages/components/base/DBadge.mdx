---
layout: ../../../layouts/DashboardLayout.astro
title: DBadge
description: Type-safe wrapper around Bootstrap Vue Next's BBadge component
category: Base Component
tags: wrapper, indicators
---

import ComponentExample from '../../../components/ComponentExample.vue';
import BootstrapVueNextReference from '../../../components/BootstrapVueNextReference.vue';
import DBadgeExample from '../../../examples/DBadgeExample.vue';
import DBadgeExampleRaw from '../../../examples/DBadgeExample.vue?raw';

# DBadge

DBadge - A type-safe wrapper around Bootstrap Vue Next's BBadge component. Small count and labelling component useful for displaying status, counts, or categories.

## Live Examples

<ComponentExample
  client:load
  code={DBadgeExampleRaw}
  exampleComponent={DBadgeExample}
/>


<BootstrapVueNextReference client:load componentName="DBadge" />
